# Build and test open-bamboo-networking on Linux x64 / aarch64 and on
# Windows x64 (MSVC + vcpkg manifest).
#
# Version strategy: the plugin must report a string whose first 8 characters
# match Bambu Studio's SLIC3R_VERSION. We therefore look up the most recent
# tag in https://github.com/bambulab/BambuStudio.git at CI time, strip the
# leading "v", and bump the last component to "99" (same rule ./configure
# uses for local auto-detection). This means CI follows upstream releases
# automatically -- no manual version bumps in this repo.
#
# Linux artifacts (one set per ABI version / architecture):
#   * libbambu_networking.so
#   * libBambuSource.so
#   * liblive555.so
#   * network_plugins.json
#
# Windows artifacts (one set per ABI version):
#   * bambu_networking.dll
#   * BambuSource.dll
#   * live555.dll (stub)
#   * network_plugins.json
#   * ota/plugins/
#
# macOS artifacts (one set per ABI version):
#   * libbambu_networking.dylib
#   * libBambuSource.dylib
#   * liblive555.dylib
#   * network_plugins.json

name: Build

on:
  push:
  workflow_dispatch:
  pull_request:
  schedule:
    - cron: '15 2 * * *'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Fast, architecture-independent check: compare the ABI surface
  # (bambu_networking.hpp + NetworkAgent.hpp + bambu_network_* dlsym list)
  # at the most recent bambulab/BambuStudio tag on github.com against the
  # baseline under tools/abi_snapshot/vMM.mm.pp/ (series from the tag).
  # See tools/check_abi_compat.sh for the full list of checks.
  abi-compat:
    name: abi-compat
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install tools
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends ca-certificates curl git

      - name: Check ABI against latest upstream Bambu Studio tag
        run: tools/check_abi_compat.sh

  build:
    name: v${{ matrix.abi }}.xx-linux-${{ matrix.arch }}
    runs-on: ${{ matrix.arch == 'x64' && 'ubuntu-24.04' || 'ubuntu-24.04-arm' }}
    strategy:
      fail-fast: false
      matrix:
        abi:
          - 02.03.00
          - 02.03.01
          - 02.04.00
          - 02.05.00
          - 02.05.01
          - 02.05.02
          - 02.05.03
          - 02.06.00
          - 02.06.01
        arch:
          - x64
          - aarch64

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake pkg-config ca-certificates git \
            libcjson-dev uthash-dev libcurl4-openssl-dev libssl-dev zlib1g-dev

      - name: Configure
        run: |
          ./configure \
            --build-type=Release \
            --enable-tests \
            --with-version='${{ matrix.abi }}.99' \
            --no-conf-patch

      - name: Build
        run: make -j"$(nproc)"

      - name: Run tests
        # The only ctest target that does real I/O is `lan_live`, and it
        # self-skips when OBN_PRINTER_IP / _SERIAL / _ACCESS_CODE are unset,
        # which is always the case here. `plugin_symbols` is a pure dlopen
        # smoke test and exercises every ABI symbol Studio looks up.
        run: make test

      - name: Verify built plugin exports every ABI symbol
        # `abi-compat` job above compares source-side files only. This step
        # additionally nm's the freshly built .so to confirm we did not
        # lose any exported entry point in this architecture's toolchain.
        run: tools/check_abi_compat.sh --so=build/libbambu_networking.so

      - name: Stage artifacts
        run: |
          set -eu
          mkdir -p dist
          cp build/libbambu_networking.so dist/
          cp build/libBambuSource.so      dist/
          cp build/liblive555.so          dist/
          cp build/network_plugins.json   dist/
          ls -l dist/

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        with:
          name: obn-linux-v${{ matrix.abi }}.xx-${{ matrix.arch }}
          path: dist/
          if-no-files-found: error

  build-windows:
    name: v${{ matrix.abi }}.xx-windows-x64
    runs-on: windows-2022
    strategy:
      fail-fast: false
      matrix:
        abi:
          - 02.03.00
          - 02.03.01
          - 02.04.00
          - 02.05.00
          - 02.05.01
          - 02.05.02
          - 02.05.03
          - 02.06.00
          - 02.06.01

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Configure
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $staging = Join-Path $env:GITHUB_WORKSPACE "install"
          New-Item -ItemType Directory -Force -Path $staging | Out-Null
          .\configure.ps1 `
            -ClientType bambu_studio `
            -WithVersion '${{ matrix.abi }}.99' `
            -Prefix $staging `
            -PatchConf:$false `
            -RegisterDShowFilter:$false `
            -EnableTests:$true

      - name: Build
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          cmake --build build --config Release --parallel

      - name: Test (probe_plugin)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          Push-Location build
          ctest -C Release --output-on-failure
          Pop-Location

      - name: Install
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          cmake --install build --config Release

      - name: Stage artifacts
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $src = Join-Path $env:GITHUB_WORKSPACE "install"
          $dist = Join-Path $env:GITHUB_WORKSPACE 'dist'
          New-Item -ItemType Directory -Force -Path $dist | Out-Null
          Copy-Item -Path (Join-Path $src 'plugins\*') -Destination $dist -Force
          $ota = Join-Path $src 'ota'
          if (Test-Path $ota) {
            Copy-Item -Path $ota -Destination (Join-Path $dist 'ota') -Recurse -Force
          }
          Get-ChildItem -Recurse $dist | Format-Table FullName,Length -AutoSize

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        with:
          name: obn-v${{ matrix.abi }}.xx-windows-x64
          path: dist/
          if-no-files-found: error

  build-macos:
    name: v${{ matrix.abi }}.xx-macos
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        abi:
          - 02.03.00
          - 02.03.01
          - 02.04.00
          - 02.05.00
          - 02.05.01
          - 02.05.02
          - 02.05.03
          - 02.06.00
          - 02.06.01

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install build dependencies
        run: |
          set -eu
          brew install pkg-config cmake openssl@3 curl cjson zlib uthash
          {
            echo "OPENSSL_ROOT_DIR=$(brew --prefix openssl@3)"
            PC="$(brew --prefix openssl@3)/lib/pkgconfig"
            PC="${PC}:$(brew --prefix curl)/lib/pkgconfig"
            PC="${PC}:$(brew --prefix zlib)/lib/pkgconfig"
            PC="${PC}:$(brew --prefix cjson)/lib/pkgconfig"
            echo "PKG_CONFIG_PATH=${PC}"
          } >> "$GITHUB_ENV"

      - name: Configure
        run: |
          ./configure \
            --build-type=Release \
            --enable-tests \
            --with-version='${{ matrix.abi }}.99' \
            --no-conf-patch

      - name: Build
        run: make -j"$(sysctl -n hw.ncpu)"

      - name: Run tests
        run: make test

      - name: Verify built plugin exports every ABI symbol
        run: tools/check_abi_compat.sh --so=build/libbambu_networking.dylib

      - name: Stage artifacts
        run: |
          set -eu
          mkdir -p dist
          cp build/libbambu_networking.dylib dist/
          cp build/libBambuSource.dylib      dist/
          cp build/liblive555.dylib          dist/
          cp build/network_plugins.json      dist/
          ls -l dist/

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        with:
          name: obn-macos-v${{ matrix.abi }}.xx
          path: dist/
          if-no-files-found: error

  build-lan-only:
    name: LAN_ONLY build test (linux-x64)
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake pkg-config ca-certificates git \
            libcjson-dev uthash-dev libssl-dev zlib1g-dev

      - name: Configure with --lan-only
        run: |
          ./configure \
            --build-type=Release \
            --enable-tests \
            --lan-only \
            --no-conf-patch

      - name: Build
        run: make -j"$(nproc)"

      - name: Run tests
        run: make test

      - name: Verify no cloud code was compiled
        run: |
          set -eu
          # Check that cloud headers were not included in compilation
          # by verifying the binary doesn't reference cloud symbols
          nm build/libbambu_networking.so | grep -q "CloudSession" && \
            { echo "ERROR: CloudSession symbol found in LAN_ONLY build"; exit 1; } || true
          echo "✓ No CloudSession symbols in LAN_ONLY build"
